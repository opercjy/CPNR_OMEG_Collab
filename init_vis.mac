# 인터랙티브 GUI 세션의 초기화를 담당하는 메인 스크립트

# 1. 커널 초기화 (필수)
# 지오메트리를 메모리에 생성하여 /gps/confine 같은 명령어가 작동하도록 합니다.
/run/initialize

# 2. 시각화 스크립트 호출
/control/execute vis.mac

# 3. 방사선원 설정 스크립트 호출
/control/execute source.mac

# 4. 라벨 설정 스크립트 호출
/control/execute label.mac

# 5. 자동 새로고침 활성화
/vis/viewer/set/autoRefresh true

# 6. 테스트 이벤트 1회 자동 실행
/run/beamOn 1
